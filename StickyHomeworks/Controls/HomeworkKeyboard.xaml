<UserControl x:Class="StickyHomeworks.Controls.HomeworkKeyboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StickyHomeworks.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <Style x:Key="KeyboardButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="6 4"/>
            <Setter Property="MinWidth" Value="36"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
        </Style>
        <Style x:Key="KeyboardActionButtonStyle" TargetType="Button" BasedOn="{StaticResource KeyboardButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidForegroundBrush}"/>
        </Style>
        <Style x:Key="CategoryHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="4 2"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBodyLight}"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <materialDesign:Card Background="{DynamicResource MaterialDesignPaper}"
                         Margin="4"
                         materialDesign:ElevationAssist.Elevation="Dp4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 数字行 -->
            <Grid Grid.Row="0" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="数字" Style="{StaticResource CategoryHeaderStyle}"/>
                <WrapPanel Grid.Column="1" x:Name="NumberPanel"/>
            </Grid>

            <!-- 中文数字行 -->
            <Grid Grid.Row="1" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="数字" Style="{StaticResource CategoryHeaderStyle}"/>
                <WrapPanel Grid.Column="1" x:Name="ChineseNumberPanel"/>
            </Grid>

            <!-- 书本行 -->
            <Grid Grid.Row="2" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="书本" Style="{StaticResource CategoryHeaderStyle}"/>
                <WrapPanel Grid.Column="1" x:Name="BookPanel"/>
            </Grid>

            <!-- 时间行1 -->
            <Grid Grid.Row="3" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="时间" Style="{StaticResource CategoryHeaderStyle}"/>
                <WrapPanel Grid.Column="1" x:Name="TimePanel1"/>
            </Grid>

            <!-- 时间行2 -->
            <Grid Grid.Row="4" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="时间" Style="{StaticResource CategoryHeaderStyle}"/>
                <WrapPanel Grid.Column="1" x:Name="TimePanel2"/>
            </Grid>

            <!-- 内容行 -->
            <Grid Grid.Row="5" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="内容" Style="{StaticResource CategoryHeaderStyle}"/>
                <WrapPanel Grid.Column="1" x:Name="ContentPanel"/>
            </Grid>

            <!-- 符号行 -->
            <Grid Grid.Row="6" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="符号" Style="{StaticResource CategoryHeaderStyle}"/>
                <WrapPanel Grid.Column="1" x:Name="SymbolPanel"/>
            </Grid>

            <!-- 常用字母行 -->
            <Grid Grid.Row="7" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="字母" Style="{StaticResource CategoryHeaderStyle}"/>
                <WrapPanel Grid.Column="1" x:Name="LetterPanel"/>
            </Grid>

            <!-- 其他行 -->
            <Grid Grid.Row="8" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="其他" Style="{StaticResource CategoryHeaderStyle}"/>
                <WrapPanel Grid.Column="1" x:Name="CustomPanel"/>
            </Grid>

            <!-- 功能键行 -->
            <Grid Grid.Row="9" Margin="2 4 2 2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="操作" Style="{StaticResource CategoryHeaderStyle}"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="← 删除" Style="{StaticResource KeyboardActionButtonStyle}"
                            Click="ButtonBackspace_OnClick"/>
                    <Button Content="↵ 换行" Style="{StaticResource KeyboardActionButtonStyle}"
                            Click="ButtonEnter_OnClick"/>
                    <Button Content="✕ 关闭" Style="{StaticResource KeyboardActionButtonStyle}"
                            Click="ButtonClose_OnClick"/>
                </StackPanel>
            </Grid>
        </Grid>
    </materialDesign:Card>
</UserControl>
